version: "2"
services:
  app:
    extends:
      file: common.yml
      service: app
    links:
     - elk
  web:
    extends:
      file: common.yml
      service: web
    links:
     - app
  elk:
    extends:
      file: common.yml
      service: elk

networks:
  default:
   external:
    name: br0
